# Fly.io config — Discord bot worker (no HTTP)
app = "arch-second-brain"
primary_region = "sin"

[build]

[restart]
  policy = "always"
  max_retries = 10

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
